# Layout Artifacts

本目录下所有文件属于 layout 阶段产物（cinematic intent、prompt pack、Blender token 等），仅 `_artifacts/` 可存放这些输出。

当前有效版本由 `../CURRENT.yaml` 指向，任何新版本必须更新该文件与 `APPROVALS.md` 中的 `CINEMATIC_INTENT_APPROVED`。
