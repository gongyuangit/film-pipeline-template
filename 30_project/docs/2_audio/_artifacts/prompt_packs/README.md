# Audio Prompt Packs

Three prompt packs correspond to Dialogue VO, SFX, and Music. Each pack follows the global + per-item negative convention (`globals.negative` + `items[].negative`). Dialogue and SFX align to shots, while Music packs align to segments/beat boundaries.
