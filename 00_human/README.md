# Human Interaction Zone
Only `00_human/` needs manual review. Codex manages `10_codex/` (tools, scripts, automation) and `20_runtime/` (generated outputs); you can ignore them unless an inbox item requests attention.
All project content now lives under `30_project/`; treat this as the “project core” and only modify it after a decision in `INBOX.md`.
请只关注 `00_human/` 中的中文内容；那些内容包含问题、选项与行动要求，正在等待你的确认。出现英文段落时，通常是技术补充，可以在必要时查看，但不需要特别解读。
