# Smoke Test
1. Run `make smoke` from the repository root.
2. Open `02_outputs/smoke/smoke_report.md` to verify the generated report.

## Plan Generation
1. Run `make plan`.
2. Check `02_outputs/plan/shot_list.json` for the generated shot list export.
